import 'package:flutter/material.dart';

class CreatePubScreen extends StatelessWidget {
  const CreatePubScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}